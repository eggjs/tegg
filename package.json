{
  "name": "tegg",
  "version": "0.0.1",
  "description": "",
  "private": true,
  "homepage": "https://github.com/eggjs/tegg",
  "bugs": {
    "url": "https://github.com/eggjs/tegg/issues"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:eggjs/tegg.git"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "lint:fix": "eslint . --ext .ts --fix",
    "lint": "eslint . --ext .ts",
    "test": "cross-env NODE_ENV=test NODE_OPTIONS='--no-deprecation' TS_NODE_TRANSPILE_ONLY=true mocha",
    "test-workspaces": "cross-env NODE_ENV=test NODE_OPTIONS='--no-deprecation' npm run test --workspaces --if-present",
    "cov": "c8 npm run test",
    "pub": "lerna publish",
    "pub-canary": "lerna publish --canary",
    "clean": "lerna run --parallel clean",
    "prepare-test": "lerna run --parallel prepare-test",
    "tsc": "lerna run tsc",
    "tsc:pub": "lerna run tsc:pub",
    "ci": "npm run prepare-test && npm run lint && npm run tsc:pub && npm run clean && npm run cov"
  },
  "author": "killagu <killa123@126.com>",
  "license": "MIT",
  "devDependencies": {
    "@eggjs/tsconfig": "^1.0.0",
    "@istanbuljs/nyc-config-typescript": "^1.0.1",
    "@types/mocha": "^10.0.1",
    "@types/node": "^14.14.31",
    "c8": "^7.12.0",
    "cross-env": "^7.0.3",
    "eslint": "^8.0.0",
    "eslint-config-egg": "^12.0.0",
    "eslint-plugin-import": "^2.24.2",
    "lerna": "^6.1.0",
    "mocha": "^10.2.0",
    "source-map-support": "^0.5.16",
    "test-exclude": "^6.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.0.0"
  },
  "workspaces": [
    "core/*",
    "plugin/*",
    "standalone/*"
  ]
}
